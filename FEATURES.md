# Новые Функции EduPoint

## ✅ Реализованные функции

### 1. Категории и теги для курсов

**Что реализовано:**
- Система категорий с иконками, цветами и сортировкой
- Система тегов с автоматической генерацией slug
- Many-to-many связь между курсами и тегами
- 6 предустановленных категорий: Программирование, Дизайн, Маркетинг, Бизнес, Языки, Наука
- 35+ предустановленных тегов: PHP, Laravel, Python, JavaScript, React, Vue.js и др.

**Как использовать:**
1. При создании курса выберите категорию из выпадающего списка
2. Выберите несколько тегов (удерживайте Ctrl)
3. На странице курса отображаются цветная badge категории и теги

**Файлы:**
- Модели: `app/Models/Category.php`, `app/Models/Tag.php`
- Миграции: `database/migrations/2025_11_12_*_create_categories_table.php` и др.
- Seeder: `database/seeders/CategoriesAndTagsSeeder.php`
- Views: `resources/views/courses/create.blade.php`, `courses/show.blade.php`

**Запуск seeder:**
```bash
php artisan db:seed --class=CategoriesAndTagsSeeder
```

---

### 2. Модули/разделы для уроков

**Что реализовано:**
- Таблица `course_modules` для группировки уроков
- Связь уроков с модулями (опционально)
- Сортировка модулей по полю `order`

**Структура:**
- Курс → Модули → Уроки
- Модуль содержит: название, описание, порядок
- Урок может принадлежать модулю (поле `module_id` nullable)

**Файлы:**
- Модель: `app/Models/CourseModule.php`
- Миграции: `database/migrations/2025_11_12_*_create_course_modules_table.php`
- Связи: `Course->modules()`, `Lesson->module()`, `CourseModule->lessons()`

**Пример использования:**
```php
// Создание модуля
$course->modules()->create([
    'title' => 'Модуль 1: Основы',
    'description' => 'Введение в курс',
    'order' => 1
]);

// Привязка урока к модулю
$lesson->update(['module_id' => $module->id]);

// Получение уроков модуля
$module->lessons; // отсортированы по order
```

**TODO:**
- [ ] Отображение модулей на странице курса
- [ ] UI для создания/редактирования модулей
- [ ] Drag-and-drop для изменения порядка

---

### 3. Комментарии к урокам

**Что реализовано:**
- Таблица `lesson_comments` для хранения комментариев
- Форма добавления комментария на странице урока
- Отображение существующих комментариев с автором и датой
- Связи: `Lesson->comments()`, `LessonComment->user()`, `LessonComment->lesson()`

**Как использовать:**
1. Авторизованные пользователи видят форму комментария под уроком
2. Введите комментарий и нажмите "Отправить"
3. Комментарии отображаются в хронологическом порядке (новые сверху)

**Файлы:**
- Модель: `app/Models/LessonComment.php`
- Миграция: `database/migrations/2025_11_12_*_create_lesson_comments_table.php`
- Controller: `app/Http/Controllers/LessonController.php` (метод `storeComment`)
- View: `resources/views/lessons/show.blade.php`
- Route: `POST /lessons/{lesson}/comments`

**Особенности:**
- Только авторизованные пользователи могут комментировать
- Комментарии привязаны к пользователю (имя автора)
- Отображается "N минут назад" / "N часов назад" (diffForHumans)
- Максимальная длина комментария: 1000 символов

**TODO:**
- [ ] Редактирование/удаление своих комментариев
- [ ] Ответы на комментарии (threading)
- [ ] Модерация комментариев преподавателем

---

### 4. Email уведомления

**Что реализовано:**
- Таблица `notifications` для хранения уведомлений
- Класс `NewLessonPublished` для уведомления о новых уроках
- Отправка через email (MailMessage) и database (в приложении)
- Иконка колокольчика в навигации с счетчиком непрочитанных
- Dropdown с последними 5 уведомлениями
- Клик по уведомлению отмечает как прочитанное и перенаправляет

**Как работает:**
1. При создании урока (`LessonController@store`) система находит всех студентов курса
2. Каждому отправляется уведомление через `ShouldQueue` (асинхронно)
3. Студент получает email с кнопкой "Перейти к уроку"
4. В навигации появляется красная badge с количеством непрочитанных
5. Клик по уведомлению открывает урок и отмечает как прочитанное

**Файлы:**
- Notification: `app/Notifications/NewLessonPublished.php`
- Controller: `app/Http/Controllers/LessonController.php` (в методе `store`)
- View: `resources/views/layouts/app.blade.php` (навигация)
- Route: `POST /notifications/{notification}/read`
- Миграция: `database/migrations/2025_11_12_*_create_notifications_table.php`

**Настройка:**
См. файл `NOTIFICATIONS_SETUP.md` для детальных инструкций по настройке SMTP и очередей.

**Краткая настройка для тестирования (Mailtrap):**
```env
MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=ваш_username
MAIL_PASSWORD=ваш_password
MAIL_ENCRYPTION=tls

QUEUE_CONNECTION=database
```

Запустите worker:
```bash
php artisan queue:work
```

**TODO:**
- [ ] Реализовать `CourseDeadlineReminder` notification
- [ ] Настройки уведомлений в профиле пользователя
- [ ] Дайджест уведомлений (weekly summary)
- [ ] Push-уведомления через WebSockets

---

### 5. Upload обложек для курсов

**Что реализовано:**
- Уже работало до этой сессии
- Поле `thumbnail` в форме создания/редактирования курса
- Валидация: image|mimes:jpeg,png,jpg,gif|max:2048
- Сохранение в `storage/app/public/thumbnails`
- Отображение на карточке курса

**Дополнительно добавлено:**
- Выбор категории при создании курса
- Выбор тегов при создании курса
- Валидация категории и тегов в `CourseController`

**Файлы:**
- Controller: `app/Http/Controllers/CourseController.php` (методы `store`, `update`)
- Views: `resources/views/courses/create.blade.php`, `courses/edit.blade.php`

---

## 📊 Статистика реализации

**Новые таблицы:** 6
- categories
- tags
- course_tag (pivot)
- course_modules
- lesson_comments
- notifications

**Новые модели:** 4
- Category
- Tag
- CourseModule
- LessonComment

**Новые notifications:** 2
- NewLessonPublished (реализован)
- CourseDeadlineReminder (создан, но пустой)

**Обновленные модели:** 2
- Course (добавлены: category_id, tags(), modules())
- Lesson (добавлены: module_id, module(), comments())

**Новые маршруты:** 2
- POST /lessons/{lesson}/comments
- POST /notifications/{notification}/read

**Обновленные views:** 3
- courses/create.blade.php (категория + теги)
- courses/show.blade.php (отображение категории + тегов)
- lessons/show.blade.php (комментарии)
- layouts/app.blade.php (уведомления в navbar)

---

## 🚀 Быстрый старт

### 1. Применить все миграции:
```bash
php artisan migrate
```

### 2. Заполнить категории и теги:
```bash
php artisan db:seed --class=CategoriesAndTagsSeeder
```

### 3. Настроить очереди (для уведомлений):
```bash
# В .env
QUEUE_CONNECTION=database

# Запустить worker
php artisan queue:work
```

### 4. Настроить email (опционально):
См. `NOTIFICATIONS_SETUP.md`

---

## 📝 Следующие шаги (из оригинального TODO)

### Приоритет 1 (базовые улучшения):
- [ ] Email verification для регистрации
- [ ] Восстановление пароля
- [ ] OAuth (Google, Microsoft)
- [ ] Admin CRUD для категорий и тегов
- [ ] Отображение модулей на странице курса
- [ ] Реализовать CourseDeadlineReminder

### Приоритет 2 (расширенные функции):
- [ ] Форум/обсуждения
- [ ] Real-time чат между студентами
- [ ] Система оплаты (Stripe/PayPal)
- [ ] Рекомендации курсов (на основе интересов)
- [ ] Gamification (баллы, достижения, leaderboard)

### Приоритет 3 (интеграции):
- [ ] Zoom/Google Meet интеграция
- [ ] Календарь событий
- [ ] Multilingual поддержка
- [ ] Расширенная аналитика и отчеты
- [ ] Экспорт данных (PDF, Excel)

---

## 🐛 Известные проблемы

1. **Дублированная миграция:** 
   - Файл `add_module_id_to_lessons_table` создан дважды
   - Обе миграции выполнены успешно
   - Не критично, но можно удалить один файл

2. **Модули не отображаются:**
   - Структура создана, но UI для отображения не реализован
   - Необходимо обновить `courses/show.blade.php`

3. **CourseDeadlineReminder пустой:**
   - Notification создан, но логика не реализована
   - Нужно определить когда и как отправлять

---

## 📚 Документация

- **Laravel Notifications:** https://laravel.com/docs/11.x/notifications
- **Laravel Queues:** https://laravel.com/docs/11.x/queues
- **Bootstrap 5 Icons:** https://icons.getbootstrap.com/

---

## 💡 Идеи для улучшения

1. **Фильтрация курсов:**
   - По категориям (в sidebar)
   - По тегам (multi-select)
   - По уровню сложности

2. **Поиск:**
   - Full-text search по названию и описанию
   - Поиск по тегам
   - Laravel Scout + Algolia/Meilisearch

3. **Прогресс обучения:**
   - Визуализация прогресса по модулям
   - Unlock следующего модуля после завершения предыдущего
   - Gamification: badges за завершение модулей

4. **Комментарии:**
   - Rich text editor (TinyMCE/Quill)
   - Mentions (@username)
   - Реакции (лайки) на комментарии

5. **Уведомления:**
   - Настройки: какие уведомления получать
   - Частота: instant / daily digest / weekly
   - Каналы: email / database / push

---

## 🔧 Команды для разработки

```bash
# Откатить последнюю миграцию
php artisan migrate:rollback

# Откатить и применить заново
php artisan migrate:refresh

# Откатить все и заполнить данными
php artisan migrate:fresh --seed

# Очистить кэш
php artisan cache:clear
php artisan config:clear
php artisan view:clear

# Посмотреть маршруты
php artisan route:list

# Создать нового пользователя через tinker
php artisan tinker
>>> User::create(['name' => 'Test', 'email' => 'test@test.com', 'password' => bcrypt('password'), 'role' => 'student'])
```

---

**Дата обновления:** 12 ноября 2025  
**Версия:** 1.1.0  
**Автор:** GitHub Copilot
